services:
  wifile:
    build: .
    ports:
      - "8765:8765" # web UI
      - "12345:12345" # transfer port (sender listens here)
    volumes:
      - ./data:/data # mount files to send/receive here
